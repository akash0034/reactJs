import React from "react";
import { Form } from "react-bootstrap";

const Home = () => {
  return (
    <React.Fragment>
      <Form.Label>Home</Form.Label>
    </React.Fragment>
  );
};

export default Home;
